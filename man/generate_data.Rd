% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_data.R
\name{generate_data}
\alias{generate_data}
\title{Generate Synthetic Data Without Duplicates for Numerical Variables}
\usage{
generate_data(num_records, num_ids, num_vars)
}
\arguments{
\item{num_records}{Integer. The total number of records to generate.}

\item{num_ids}{Integer. The total number of unique IDs to generate.}

\item{num_vars}{Integer. The total number of variables to generate.}
}
\value{
A list containing the following elements:
\item{df}{A \code{data.table} with the generated dataset, including \code{ID}, \code{t}, \code{var}, and \code{value}.}
\item{df_population}{A \code{data.table} with the population data (unique IDs).}
\item{var_type_override}{A named vector indicating whether each variable is "numerical" or "categorical".}
}
\description{
This function generates a synthetic dataset with specified numbers of records, IDs,
and variables. Numerical variables are ensured to have no duplicates across \code{ID},
\code{t}, and \code{var}. It also generates metadata for variable types and population data.
}
\examples{
# Generate a dataset with 1000 records, 100 unique IDs, and 50 variables
data_list <- generate_data(num_records = 1000, num_ids = 100, num_vars = 50)
df <- data_list$df
df_population <- data_list$df_population
var_type_override <- data_list$var_type_override

# Inspect the generated data
head(df)
head(df_population)
var_type_override
}
